# ─────────────────────────────────────────
# Test Data — SauceDemo
#
# SECURITY: sensitive values (LOGIN_PASS, tokens, API keys) should be supplied
# as environment variables.  TestDataResolver checks env vars FIRST before
# falling back to this file.  In CI/CD, inject secrets via pipeline secrets,
# not hardcoded here.  This file is committed only because it holds public
# demo credentials for saucedemo.com — replace with env vars for real projects.
# ─────────────────────────────────────────
LOGIN_USER=standard_user
LOGIN_PASS=secret_sauce
BASE_URL=https://www.saucedemo.com

# ─────────────────────────────────────────
# Test Data — ReqRes API
# ─────────────────────────────────────────
API_BASE_URL=https://reqres.in